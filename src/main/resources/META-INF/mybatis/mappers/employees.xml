<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.web.mapper.EmployeeDao">

    <select id="getEmployeesByJobId" parameterType="string" resultType="Employee">
        select
            employee_id     as id,
            first_name      as firstName,
            last_name       as lastName,
            email,
            phone_number    as phoneNumber,
            hire_date       as hireDate,
            job_id          as "job.id",
            salary,
            commission_pct  as commissionPct,
            manager_id      as "manager.id",
            department_id   as "department.id"
        from
            employees
        where
            job_id = #{job_id}
    </select>

    <select id="getEmployeeById" parameterType="int" resultType="Employee">
        select
            employee_id     as id,
            first_name      as firstName,
            last_name       as lastName,
            email,
            phone_number    as phoneNumber,
            hire_date       as hireDate,
            job_id          as "job.id",
            salary,
            commission_pct  as commissionPct,
            manager_id      as "manager.id",
            department_id   as "department.id"
        from
            employees
        where
            employee_id = #{employee_id}
    </select>

    <select id="getAllEmployees">
        select
            employee_id     as id,
            first_name      as firstName,
            last_name       as lastName,
            email,
            phone_number    as phoneNumber,
            hire_date       as hireDate,
            job_id          as "job.id",
            salary,
            commission_pct  as commissionPct,
            manager_id      as "manager.id",
            department_id   as "department.id"
        from
            employees
        order by
            employee_id
    </select>

    <select id="getEmployeesByDepartmentId" parameterType="int" resultType="Employee">
        select
            employee_id     as id,
            first_name      as firstName,
            last_name       as lastName,
            email,
            phone_number    as phoneNumber,
            hire_date       as hireDate,
            job_id          as "job.id",
            salary,
            commission_pct  as commissionPct,
            manager_id      as "manager.id",
            department_id   as "department.id"
        from
            employees
        where
            department_id = #{department_id}
        order by
            first_name, last_name
    </select>

    <insert id="insertEmployee" parameterType="Employee">
        insert into employees (
            first_name, last_name, email, phone_number, hire_date, job_id, salary, commission_pct, manager_id, department_id
        )
        values (
            #{firstName}, #{lastName}, #{email}, #{phoneNumber}, #{hireDate}, #{job.id}, #{commissionPct}, #{manager.id}, #{department.id}
        )
    </insert>
</mapper>